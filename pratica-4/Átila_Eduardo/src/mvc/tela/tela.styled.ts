import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    texto: {
        fontSize: 18,
        color: "black",
        textAlign: 'auto',
        borderRadius: 10,
        borderWidth: 2,
    },

});
export { styles };